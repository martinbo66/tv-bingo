# TV Bingo Frontend Environment Variables
# Copy this file to .env.local for local development

# Backend API base URL
# - For local dev with separate frontend (npm run dev) and backend (gradlew bootRun):
#   VITE_API_BASE_URL=http://localhost:8080
# - For production/Kubernetes (frontend served by backend): leave unset or empty
VITE_API_BASE_URL=http://localhost:8080
